version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm install
      - npm run build
run:
  command: node server/server.js
  network:
    port: 5000
  env:
    - name: NODE_ENV
      value: production
